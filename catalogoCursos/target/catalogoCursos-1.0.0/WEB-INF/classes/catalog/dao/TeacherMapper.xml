<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
	
<mapper namespace="catalog.dao.TeacherMapper">
	<select id="selectAll" resultType="catalog.model.Teacher">
		select * from teachers
	</select>
</mapper>